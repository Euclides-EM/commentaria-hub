{
  "name": "hub-api",
  "version": "1.0.0",
  "scripts": {
    "gen-api": "rm -rf hub-api/ && openapi -i ../../ocrflow/internal/docs/swagger.json -c axios -o ./hub-api --useOptions --useUnionTypes"
  },
  "dependencies": {
    "axios": "^1.13.2",
    "form-data": "^4.0.5"
  },
  "devDependencies": {
    "openapi-typescript-codegen": "^0.30.0"
  }
}
